name: CICD

on:
  push:
    branches:
      - dev

jobs:
  build:
    name: Deploy Vite
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'

      - name: npm install, build, and test
        run: |
          npm install
          npm run build --if-present
          npm run test --if-present

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: battlebyte-frontend
          path: dist

  deploy:
    permissions:
      contents: none
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: battlebyte-frontend

      - name: show files
        run: ls

      # - name: Stop server
      #   env:
      #     PASSWORD: ${{secrets.PASSWORD}}
      #     USERNAME: ${{secrets.USERNAME}}
      #     HOST: ${{secrets.HOST}}
      #   run: sshpass -p $PASSWORD ssh -o StrictHostKeyChecking=no $USERNAME@$HOST "cd /home/ubuntu/BattleByte-backend && ps -ef | grep BattleByte.jar | grep -v grep | awk '{print $2}' | xargs -r kill -9"

      # - name: Upload
      #   env:
      #     PASSWORD: ${{secrets.PASSWORD}}
      #     USERNAME: ${{secrets.USERNAME}}
      #     HOST: ${{secrets.HOST}}
      #   run: sshpass -p $PASSWORD scp -r -o StrictHostKeyChecking=no ./target/BattleByte.jar $USERNAME@$HOST:/home/ubuntu/BattleByte-backend

      # - name: Restart server
      #   env:
      #     PASSWORD: ${{secrets.PASSWORD}}
      #     USERNAME: ${{secrets.USERNAME}}
      #     HOST: ${{secrets.HOST}}
      #   run: sshpass -p $PASSWORD ssh -o StrictHostKeyChecking=no $USERNAME@$HOST "cd /home/ubuntu/BattleByte-backend && nohup java -jar BattleByte.jar 1> console.log 2> error.log & && pwd"
